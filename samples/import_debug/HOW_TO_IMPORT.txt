Power Sweeper — import debug (Studio "unknown error")
=====================================================

Root causes (seen on failing cleans)
------------------------------------
1) YAML shape: Symfony dump quoted Power Fx (`Fill: '=…'`) and broke Children lists.
2) ZIP packaging: PHP ZipArchive on Linux stamped Unix metadata (create_system=3).
   Studio wants DOS/Windows (create_system=0), Header.json + Properties.json first.
3) JSON shape: PHP json_encode pretty-print used LF + 4 spaces; Studio exports
   CRLF + 2 spaces. Wrong JSON formatting → ErrOpeningDocument_UnknownError.

Current Power Sweeper: DOS zip writer, PowerAppsYaml, StudioJson (CRLF/2-space).

What to try (in order)
----------------------
Plain app that imports today:
  CDLS (L) VCR App (4).msapp

Stepped rebuilds from that plain app (same folder / steps/):
  steps/CDLS_VCR_01_empty_pipeline.msapp     — unpack/repack only
  steps/CDLS_VCR_02_unwhack_only.msapp
  steps/CDLS_VCR_03_dark_mode_only.msapp
  steps/CDLS_VCR_04_unwhack_then_dark.msapp
  steps/CDLS_VCR_05_repair_studio_errors.msapp
  steps/CDLS_VCR_06_repair_then_dark.msapp

Recommended new outputs:
  CDLS_L_VCR_App.repair_only.msapp           — repair hops only
  CDLS_L_VCR_App.repair_then_dark.msapp      — repair then dark mode

Powered deliverable (repair + theme — preferred for Studio import):
  CDLS_L_VCR_App_repair2.powered.msapp       — from repair2 baseline
  CDLS_L_VCR_App_16.powered.msapp            — from App (16) baseline

Repair-only deliverable (no dark mode, fresh SARIF):
  CDLS_L_VCR_App_repair2.repaired.msapp       — php scripts/build_repaired.php

Validate a powered pack locally:
  php scripts/validate_powered.php samples/import_debug/CDLS_L_VCR_App_repair2.powered.msapp

Compare before/after repair patterns:
  php scripts/compare_repair2_patterns.php

YAML-only repair of the previously failing cleaned packs
(re-serialize formulas/Children; keeps prior hop edits):
  CDLS_L_VCR_App_20260725.cleaned.REPAIRED_YAML.msapp
  VCDS_THCEE_20260725.cleaned.REPAIRED_YAML.msapp

Still-broken originals (for comparison; expect Studio unknown error):
  CDLS_L_VCR_App_20260725.cleaned.msapp
  VCDS_THCEE_20260725.cleaned.msapp

Import
------
1. make.powerapps.com → Apps → Import app → From file (.msapp)
2. Prefer *.powered.msapp (full repair + gblTheme toggle) or REPAIRED_YAML / repair_then_dark
3. Open in Studio and Save once (refreshes embedded AppCheckerResult.sarif)

App checker counts
------------------
- Original App (16) inventory: 1112 formulas, 476 accessibility (not 476 formulas).
- Stale embedded SARIF on pre-powered intermediates can show hundreds of phantom
  formula errors until Save — use regenerate_sarif hop or import *.powered.msapp.
- Filter Studio App checker to "Formulas" when verifying repair progress.

Two-step workflow (profiles, not combined):
1. Profile repair_studio_errors → download
2. Profile dark_mode on that output → download
(Requires Power Sweeper build with PowerAppsYaml fix.)
